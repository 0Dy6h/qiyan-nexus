# Runtime literature state

本目录用于保存运行时文献状态副本，例如 PDF 上传 metadata、parse status 与 parse result。

- `literature_state.json` 是运行态文件，不提交。
- seed 数据仍在 `backend/data/literature/sample_*.json`。
- 清空本目录会让应用重新从 seed 数据 bootstrap。
- 不要把本目录内容作为 fixture 修改提交。
