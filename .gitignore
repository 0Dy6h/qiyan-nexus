# Python
__pycache__/
*.py[cod]
.pytest_cache/
.pytest-tmp/
backend/.pytest-tmp/
.tmp/
.mypy_cache/
.ruff_cache/
.venv/
venv/
*.egg-info/

# Node / Next.js
node_modules/
.next/
out/
dist/
coverage/
*.tsbuildinfo

# Playwright artifacts (A4 E2E)
frontend/test-results/
frontend/playwright-report/
frontend/blob-report/
frontend/playwright/.cache/

# Backend runtime uploads
backend/uploads/**
!backend/uploads/README.md

# Local reviewer-only PDF samples
local-review-pdfs/

# Backend runtime literature state
backend/data/runtime/**
!backend/data/runtime/README.md

# Environment / secrets
.env
.env.*
!.env.example
infra/postgresql-spike.env.local

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# CodeGraph local index (per-machine, never commit)
.codegraph/

# OpenCode local config (per-machine MCP paths, never commit)
opencode.json
opencode.jsonc
.opencode/

# Codex local hooks/config (per-machine agent tooling)
.codex/

# Claude Code hook runtime state (per-session Read tracking)
.claude/hooks/.enforce-codegraph-state.json

# Trae temporary documents (session-specific planning)
.trae/

# Local-only work tracks (paper output, project entity archive)
论文产出/
项目实体/
