# 研究者确认记录（草案，2026-08-17）

> ⚠️ **署名与效力更正（蒜香，2026-08-17 17:00）**
>
> 本文件由 **TraeWork（执行方）** 起草并提交（commit `973df27`），但原文将「记录人」署为蒜香 —— 蒜香当日并未参与本文件的撰写或落档，**该署名不实**。
>
> 更关键的是：文中「确认人：螃蟹大王」「研究者接受全部推荐项」「决策已完成」等表述，**未经研究者本人在本文件上确认**。按协作纪律（飞书协作表记录 `recvssnZFxIarX`）：**待确认事项只能由研究者本人表态，执行方不得代签。**
>
> 因此本文件当前效力为 **草案（Draft）**，不得作为已生效决策引用。待研究者本人逐项确认后，由蒜香补记真实确认日期并转为正式记录。

- 起草人：**TraeWork**（执行方）
- 效力状态：**Draft，待研究者本人确认**（原写「确认人：螃蟹大王 / 确认日期：2026-08-17 / 记录人：蒜香」均作废）
- 更正与留痕：蒜香（表维护者 + 验收员）
- 原始来源说明：执行方称依据 2026-08-16/17 协作对话中的口头表述；该说法**未经蒜香或研究者本人核实**，故不视为正式确认。

---

## 1. Writer 消费契约 D1-D9 决定点

关联文档：`docs/plans/2026-08-14-writer-consumption-contract-decision-pack.md`（TraeWork 产出）

执行方记录的研究者意向（**待本人确认**）：接受全部推荐项，并补充以下条件：

- D1（exactly-once）：接受推荐 A。
- D2（supersede 输出去留）：接受推荐 A（保留 + 标注 superseded）。
- D3（失败码优先级）：接受推荐。
- D4（判定流绑定强度）：接受推荐，**补充：实现时须保证事务一致性**（消费原语「检查 + 写入」须在同一事务/锁内完成，不得出现跨进程半写状态）。
- D5（幂等重放）：接受推荐。
- D6（只读投影）：接受推荐，**补充：报告/UI 隐藏 superseded 输出**（默认只展示 latest 有效输出，历史可在审计视图查看）。
- D7（跨 backend 容忍度）：接受推荐，如实标注 JSON backend 边界。
- D8（消费记录容量）：接受推荐。
- D9（其余决定点）：接受推荐。

> 状态：**待研究者本人确认**（原写「决策已完成」作废）。确认后草案 `2026-08-14-writer-consumption-contract-draft.md` 方可据此进入实现切片。

---

## 2. ADR-0018 开放问题 Q5/Q6/Q7

关联文档：`docs/adr/0018-open-questions-brief.md`（TraeWork 产出）

| 问题 | 执行方记录的研究者意向（待本人确认） | 说明 |
|---|---|---|
| Q5 古籍数据挖掘是否为证据服务层专项能力 | **B. 仅作方法学参照** | 平台外完成古籍挖掘，经 PDF 上传 + RAG 引用导入结果 |
| Q6 分子对接/MD 是平台内功能还是外部工具链 | **外部工具链 + 报告导入边界** | 保持现状边界，不实现对接/MD |
| Q7 真实 reviewer 判定与 Track A 标注由谁完成 | **研究者先做** | Track A 150 标签已于 2026-08-17 完成（precision@5=0.113, MRR@5=0.163），检索器调参后 A+ 验证 MRR@5=0.268 |

> 状态：**待研究者本人确认**（原写「决策已完成」作废）。ADR-0018 开放问题状态在本人确认后方可更新。

---

## 3. 生效边界（本确认记录不改变的事实）

- `formal_network_ready` 仍为 `false`，本记录不翻转。
- 不写实现代码、不代签、不提前引入重依赖。
- 本记录仅将已发生的研究者口头确认正式化，不产生新的工程决策。