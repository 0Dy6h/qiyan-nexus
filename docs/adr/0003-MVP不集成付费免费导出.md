# MVP 不集成付费，所有导出免费

> **状态：历史规划提案。** 仅保留早期产品取舍；当前导出能力和商业边界以 `docs/current-state.md` 与实际 endpoint 为准。

MVP 内测期间（50 人规模）所有导出功能免费，不集成支付模块。阶段 2 根据内测反馈和数据再决定定价策略和支付集成。

**为什么不在 MVP 做付费**：集成支付（微信/支付宝）增加合规复杂度和开发时间。内测阶段用量可控（Claude 日配额控制），导出收入不足以覆盖支付集成成本。免费导出有助于收集用户真实需求，为阶段 2 定价提供数据支撑。
