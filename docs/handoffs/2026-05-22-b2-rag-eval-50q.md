# B2 — RAG eval 扩展到 50 题（2026-05-22）

> 阶段 B 第二颗 slice。30 道新题 + 8 条新 chunk + provider_name 透传到 eval report。
> 前置文档：`docs/handoffs/2026-05-22-b2-prep-notes.md`。

## 落地点

- **eval 数据**：`backend/data/evals/rag_ad_eval_questions.json` 由 20 题扩到 50 题（rag-eval-021 ~ rag-eval-050）。
- **chunk seed**：`backend/data/literature/sample_ad_chunks.json` 由 12 条扩到 20 条；之前没 chunk 的 8 篇文献（cn-ad-pruritus-005 / cn-ad-barrier-006 / pmid-40100004 / pmid-40100006 / pmid-40100007 / pmid-40100008 / pmid-40100009 / pmid-40100010）均补齐主题对应 chunk。
- **provider_name 透传**：
  - `RagAnswerResponse.provider_name: str` 新增公共字段，从 `AnswerDraft.provider_name` 写入。
  - `RagEvalItemResult.provider_name` 与 `RagEvalSummary.provider_name` 同步新增，方便 C1 真接 Claude 后做基线对比。
- **测试更新**：
  - `test_eval_service.py` 9 处硬锁解锁为 50/>=48/>=45/byte-identical；新增 `test_rag_eval_report_tags_default_provider_name`。
  - `test_eval_api.py` 两条 endpoint 测试改为 == 50。
  - `test_rag_api.py` `available_citation_count` 由 16 → 17（新 chunk 让某 lit 多出 1 个 chunk 候选）。
  - 前端 `rag-export.test.ts` SAMPLE_RESULT 加 `provider_name: "deterministic"`；`lib/api/rag.ts` 同步类型。

## 30 题主题分布（实际）

| 簇 | 题号 | 命中策略 |
|---|---|---|
| Formula 复方机制 | q021–q024 (cn) / q025–q026 (en) | 强匹配 cn-ad-formula-002 / cn-ad-barrier-006 / pmid-40100004 |
| Target & Pathway | q027–q034 | JAK-STAT / STAT 磷酸化 / type 2 / IL-31 / NF-κB / filaggrin / PI3K-Akt / targeted therapy |
| Microbiome | q035–q038 | cn-ad-microbiome-003 / pmid-40100002 / pmid-40100007 / pmid-40100009 |
| Pediatric | q039–q041 | cn-ad-child-009 / pmid-40100010 |
| Barrier & External | q042–q045 | cn-ad-external-008 / cn-ad-barrier-006 / pmid-40100006 |
| Evidence map / Review | q046–q048 | cn-ad-review-010 / pmid-40100004 |
| Network pharmacology | q049–q050 | cn-ad-network-007 / pmid-40100008 |

## 调试痕迹（must_include 反查）

第一次跑 50 题报告 pass_rate = 47/50。三处 fail 都是 must_include 拼写未对齐 citation 文本字面：

| 题号 | 误用 token | 改为 |
|---|---|---|
| rag-eval-028 | `targeted therapy` | `therapeutic axis`（chunk 仅含 `targeted therapy strategies`，但 source_quote 只到 `therapeutic axis`） |
| rag-eval-034 | `targeted therapy` | `therapeutic axis`（同上） |
| rag-eval-048 | `outcome measures` | `clinical studies`（snippet 只有 `outcomes`，没有 `outcome measures`） |

调整后 50/50 pass，chunk_hit_count = 50，citation_hit_count = 50，disclaimer_coverage = 50，must_not_violation = 0。

## 行为契约

| 维度 | 行为 |
|---|---|
| eval 题目总数 | 50（rag-eval-001 ~ rag-eval-050） |
| baseline pass rate | 实际 50/50，断言锁 `>= 48` |
| chunk_hit_count | 实际 50/50，断言锁 `>= 45` |
| citation_hit_count | 实际 50/50，断言锁 `>= 48` |
| disclaimer_coverage_count | `== 50` |
| must_not_violation_count | `== 0` |
| q019 lit-hit 三元组 | 保留 `[cn-ad-network-007, pmid-40100008, pmid-40100005]` |
| `RagAnswerResponse.provider_name` | 默认 `"deterministic"`；`QIYAN_LLM_PROVIDER=mock_claude` 时为 `"mock_claude"` |
| `summary.provider_name` | 反映本次 eval run 实际用的 provider；C1 真接 Claude 时无需改 eval 代码 |

## 不在 B2 范围（保留 prep notes 边界）

- 不引入真实 embedding / 向量检索（C3）
- 不改 `DISCLAIMER` 字符串
- 不调 `_KEYWORD_ALIASES` 别名表
- 不接 LLM provider 对比 eval（仅记录 provider_name，不切换运行）
- 不在 `/rag` 页面回显 provider_name（roadmap 未列；可在 B6 或独立小 slice 中做）

## 验证

```bash
cd backend
.venv/bin/python -m ruff format --check app tests
.venv/bin/python -m ruff check app tests
.venv/bin/python -m mypy app
.venv/bin/python -m pytest -q
# 143 passed (+1)

cd frontend
pnpm test       # 81 → 同等数量（仅 mock 字段扩展）
pnpm typecheck  # silent OK
pnpm build      # 7 routes prerender
```

## 下一颗候选

- **B3**：网络药理学任务壳（roadmap 估 3d）——开始 MVP-B 数据模型落地。
- **B6**：数据来源切换面板（roadmap 估 0.5d）——快速 UI 收益；同时可在合规页加上 provider 维度提示。
- 若想先做 UI 透传：可在 `/rag` 页面回显 `provider_name`（与 B6 合并是顺手活）。

按 roadmap §3.2 顺序，下一颗推荐 **B3**。
