# Session Wrap + Handoff — 2026-06-01 (Slices 1-5)

branch: feat/l2-real-llm-promotion (13 commits ahead of origin/main; 5 new today)
default RAG path: offline deterministic, unchanged all session
gauntlet at stop: backend 324 passed, mypy/ruff clean

---

## The arc of this session

Goal was the L2 promotion execution plan (real-answer validation set → NLI
evaluation → batching → §4c walkthrough prep). Delivered 5 slices:

### Slice 1 — Real-answer claim capture (cf3d6e5)
- `backend/scripts/capture_real_answer_claims.py`: live (opencode_go) and
  offline (pre-recorded smoke claims) modes
- Offline mode captures 7 real claims from the 2026-05-31 smoke + deterministic
  chunk citations for pairing
- Output to `backend/data/runtime/captured_real_claims_*.json` (gitignored)

### Slice 2 — Human-annotated validation set (04a494a)
- `backend/data/evals/grounding_real_answer_pairs.json`: 20 pairs
  - 7 real LLM claims (faithful + partial support)
  - 13 authored hard negatives (cross-topic mismatch, fabricated efficacy,
    scope overreach, negation, entity splicing)
  - Labels: 4 supported, 3 partial, 13 unsupported
- `backend/tests/test_real_answer_pairs_fixture.py`: 8 structure validation tests

### Slice 3 — NLI real-distribution evaluation (2a1d764)
- `RealAnswerPair` schema + `run_nli_real_distribution_eval()` in eval service
- `scripts/eval_nli_real_distribution.py` runner
- **Result: 0 false accepts, 0 false rejects, gap +0.9549**
  - supported: 0.9966–0.9990 (mean 0.9981)
  - unsupported: 0.0001–0.0417 (mean 0.0042)
  - Threshold 0.5 is extremely conservative (any [0.05, 0.99] works)
- See `docs/evaluations/2026-06-01-nli-real-distribution.md`

### Slice 4 — Per-answer NLI batching (959acba)
- `entailment_batch()` on NliBackend Protocol + TransformersNliBackend
- grounding.py NLI gate now batches all claim-ref pairs in one forward pass
- Bench: 3-claim ~2130ms (batch) vs ~2417ms (individual), ~1.1x speedup
- `bench_nli_latency.py` extended with batch comparison

### Slice 5 — §4c walkthrough preparation (496be82)
- `docs/checklists/internal-preview-smoke.md` §4c section: enablement steps,
  7 verification checkpoints, expected findings table
- `docs/current-state.md` updated: Slices 1-4 marked complete, §4c remaining

### T0 — Branch hygiene
- `feat/l2-real-llm-promotion` pushed to origin
- `chore/gitignore-agents-update` created + pushed for the 2 pre-existing edits

---

## Current state (facts)

- Default RAG: `deterministic`, offline, no key, no egress. Never changed.
- Real provider: L1 (controlled smoke/demo) per ADR-0012. No default flip.
- NLI gate: implemented, default OFF. Enable: `QIYAN_NLI_BACKEND=transformers` +
  `QIYAN_NLI_THRESHOLD=0.5`.
- **L2 engineering prerequisites: COMPLETE.** Threshold validated on real
  distribution (0 FP/0 FN); batching reduces overhead. Only remaining blocker is
  §4c human reviewer walkthrough.
- Hard invariants intact: disclaimer byte-identical, gate-on-for-external,
  safe fallback, secret-only-env.

---

## L2 promotion status (ADR-0012 §4)

| Prerequisite | Status |
|---|---|
| §4a — Threshold recalibration | ✅ NLI gate at 0.5 validated on real-answer distribution (0 FP, 0 FN) |
| §4b — SLI baseline | ✅ NLI latency measured (cold ~6s, warm batch ~2.1s/3-claim). Real prices: config-dependent (default 0.0). |
| §4c — Human reviewer walkthrough | ⬜ Walkthrough steps documented in checklist §4c; **needs real human reviewer** |

---

## Recommended next action

**Execute §4c walkthrough** or **decide on Slice 6 (default flip)**. If the human
reviewer is available:
1. Follow the §4c enablement steps in `docs/checklists/internal-preview-smoke.md`
2. Verify all 7 checkpoints (R1-R7)
3. Record findings in the completion table
4. If all pass → Slice 6: flip default to `opencode_go` with NLI gate on,
   update ADR-0012, runbook, current-state.md

If human reviewer is NOT available now, the branch can be pushed/merged and the
walkthrough deferred — the engineering work is independently complete.

---

## Git status

- Branch: `feat/l2-real-llm-promotion` (13 commits ahead of `origin/main`)
- 5 new commits in this session: cf3d6e5 through 496be82
- Branch is pushed to origin

## Key files this session

New:
- `backend/scripts/capture_real_answer_claims.py`
- `backend/data/evals/grounding_real_answer_pairs.json`
- `backend/tests/test_real_answer_pairs_fixture.py`
- `backend/scripts/eval_nli_real_distribution.py`
- `docs/evaluations/2026-06-01-nli-real-distribution.md`

Modified:
- `backend/app/services/eval.py` (RealAnswerPair path + NLI eval function)
- `backend/app/schemas/eval.py` (RealAnswerPair model)
- `backend/app/services/grounding.py` (batch NLI in grounding gate)
- `backend/app/services/nli.py` (entailment_batch)
- `backend/scripts/bench_nli_latency.py` (batch comparison)
- `backend/tests/test_grounding_nli.py` (FakeNliBackend.entailment_batch)
- `docs/checklists/internal-preview-smoke.md` (§4c walkthrough)
- `docs/current-state.md` (L2 progress updated)
